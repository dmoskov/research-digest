__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
# Never commit local credentials: every secret this package reads comes from the
# environment (ANTHROPIC_API_KEY, DIGEST_DB_*, GEMINI_API_KEY).
.env
.env.*
*.pem
*.key
.pytest_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
.digest-state.json
.DS_Store

# Transient staging tooling (regenerates this tree from the monorepo copy);
# obsolete once this repo is the source of truth.
.stage.sh
.transform.py
*.src
